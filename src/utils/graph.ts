import * as State from 'state'


interface IVertex {
    key: number

}

export function findClosestPath(junctions: State.IMapGraph, fromCities: string[], toCities: string[]): State.IMapJunction {
    return {}
}
